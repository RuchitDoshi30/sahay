/**
 * routes.js — Canonical route paths for the Sahay 4-step citizen journey.
 * Use these constants instead of hard-coded strings in navigate() calls.
 */

export const ROUTES = {
  HOME: '/',
  PROFILE: '/profile',
  RESULT: '/result',
  PARTNERS: '/partners',
  SUMMARY: '/summary',
}

export default ROUTES
